generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "rhel-openssl-1.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String    @id @default(uuid())
  email     String    @unique
  username  String    @unique
  password  String?                   // nullable: usuários Google não têm senha
  googleId  String?   @unique         // ID único retornado pelo Google
  createdAt DateTime  @default(now())
  palpites  Palpite[]
}

model Jogo {
  id           Int       @id
  homeTeamId   Int
  homeTeamName String
  awayTeamId   Int
  awayTeamName String
  utcDate      DateTime
  status       String
  homeScore    Int?
  awayScore    Int?
  groupName    String?
  palpites     Palpite[]
}

model Palpite {
  id        String   @id @default(uuid())
  userId    String
  jogoId    Int
  homeScore Int
  awayScore Int
  pontos    Int?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
  jogo Jogo @relation(fields: [jogoId], references: [id], onDelete: Cascade)

  @@unique([userId, jogoId])
}
